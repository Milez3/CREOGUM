/**
 * Legacy hook — the scroll reveal, counters, bar fills, and parallax it
 * once managed now live inside motion primitives under
 * `components/creogum/motion/*`. Kept as a no-op re-export so any
 * leftover imports fail loudly rather than silently misbehave.
 */
export function useCreogumSite(): void {
  // Intentionally empty — migrated to Framer Motion primitives.
}
